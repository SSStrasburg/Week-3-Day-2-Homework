

function fizzBuzz() {

  var countTo = input;
  for(var i=1;i<=countTo;i++){
    		output = '';

        if( i%3 === 0 ) {
          output += Fizz
        }

        if( i%5 === 0 ) {
          output += Buzz
        }

        print(output);
  }
}
